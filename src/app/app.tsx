import type { FC } from "react";
import { ReactLibraryTemplate } from "../lib/components/react-library-template";
import "./index.css";

const App: FC = () => {
	return (
		<div>
			<ReactLibraryTemplate />
		</div>
	);
};

export default App;
